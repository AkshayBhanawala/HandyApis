export const API_PREFIX = '/api';
export const PATHS = {
	healthCheck: `${API_PREFIX}/health-check`,
	read300StatusResponse: `${API_PREFIX}/read-300-status-response`,
	users: `${API_PREFIX}/users`,
};
